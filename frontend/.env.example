# Same-origin proxy (see vite.config.ts). Docker Compose sets "/api" + VITE_PROXY_TARGET.
VITE_API_URL=/api
# Local dev without Docker: omit or use http://127.0.0.1:4000 only if you skip the proxy.
# VITE_PROXY_TARGET=http://127.0.0.1:4000
